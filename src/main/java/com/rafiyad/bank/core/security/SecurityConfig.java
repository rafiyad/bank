package com.rafiyad.bank.core.security;



//@Configuration
public class SecurityConfig {

/*
    @Bean
    public ReactiveUserDetailsService reactiveUserDetailsService() {
        // Call a user from database
        // Find the user if exist on the database. if exist check the password he/she provided and password from the database

        var u1 = User.withUsername("rafiyad")
                .password(passwordEncoder().encode("1234"))
                .authorities("read")
                .build();

        return new MapReactiveUserDetailsService(u1);
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
*/


}
